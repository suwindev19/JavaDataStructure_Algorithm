package codeChef;
import java.util.Map;
import java.util.Scanner;
public class GC_2_Q {
    public static void main(String[] args) {


    }
}
















/*
Scanner sc = new Scanner(System.in);
        int N = sc.nextInt();
        int[] array = new int[N];
        for(int i =0; i < N; i++){
            array[i] = sc.nextInt();
        }
        int Q = sc.nextInt();
        while (Q > 0){
            int L = sc.nextInt();
            int R = sc.nextInt();
            L = L - 1;
            R = R - 1;
            int min = Integer.MAX_VALUE;
            for (int i = L ; i < R; i++){
                for(int j = i +1; j <= R ; j++){
                    int curr = Math.abs(array[i] - array[j]);
//                    if (curr < min){
//                        min = curr;
//                    }
                    min = Math.min(curr, min);
                }
            }
            System.out.println(min);
            Q--;
 */